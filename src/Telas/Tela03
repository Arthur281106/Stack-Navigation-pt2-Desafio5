import React from 'react';
import { View, Text, Image, Button } from 'react-native';
import { useNavigation } from '@react-navigation/native';
import { estilos } from '../styleSheet/estilos';

export default function Tela03() {
  const nav = useNavigation();

  return (
    <View style={estilos.avatarFundo}>
      <Text style={estilos.avatarTitulo}>Zuko</Text>

      <Image
        source={{ uri: 'https://static.wikia.nocookie.net/avatar/images/4/4b/Zuko.png/revision/latest/scale-to-width/360?cb=20180630112142' }}
        style={estilos.avatarImg}
      />

      <Text style={estilos.avatarTexto}>Príncipe da Nação do Fogo.</Text>
      <Text style={estilos.avatarTexto}>• Inicia como antagonista.</Text>
      <Text style={estilos.avatarTexto}>• Busca redenção e honra.</Text>
      <Text style={estilos.avatarTexto}>• Uma das jornadas mais profundas da série.</Text>

      <View style={estilos.botoes}>
        <Button title="Voltar" onPress={() => nav.navigate('Tela02')} />
      </View>
    </View>
  );
}
