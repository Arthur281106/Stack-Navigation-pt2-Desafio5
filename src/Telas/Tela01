import React from 'react';
import { View, Text, Image, Button } from 'react-native';
import { useNavigation } from '@react-navigation/native';
import { estilos } from '../styleSheet/estilos';

export default function Tela01() {
  const nav = useNavigation();

  return (
    <View style={estilos.avatarFundo}>
      <Text style={estilos.avatarTitulo}>Aang</Text>

      <Image
        source={{ uri: 'https://upload.wikimedia.org/wikipedia/pt/thumb/8/86/Avatar_Aang.png/250px-Avatar_Aang.png' }}
        style={estilos.avatarImg}
      />

      <Text style={estilos.avatarTexto}>O último dominador de ar e Avatar destinado a trazer equilíbrio ao mundo.</Text>
      <Text style={estilos.avatarTexto}>• Alegre e pacifista.</Text>
      <Text style={estilos.avatarTexto}>• Mestre dos quatro elementos.</Text>
      <Text style={estilos.avatarTexto}>• Carrega a responsabilidade de salvar o mundo.</Text>

      <View style={estilos.botoes}>
        <Button title="Voltar" onPress={() => nav.navigate('Principal')} />
        <Button title="Próximo" onPress={() => nav.navigate('Tela02')} />
      </View>
    </View>
  );
}
